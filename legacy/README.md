# Legacy static site (archived)

This folder is the pre-Next.js HTML/CSS/JS site kept for reference during migration review.

Do **not** deploy these files as the live site. Secrets that previously lived in client JS have been redacted.

The active application is the Next.js app at the repository root.
